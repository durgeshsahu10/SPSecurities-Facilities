> [!IMPORTANT]
> Avoid rewriting published git history — force pushing, or
> rebasing/amending/squashing commits that are already pushed — as it can
> cause collaborators to lose their project history.
>
> Keep the branch in a working state so every commit stays deployable.
